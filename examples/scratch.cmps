//{ x: { x: }, y: { y: { 2 }} => { println(x, { y }) } }()
//println("Map literal", {:}, {x:}, {x, y: 2}, {"x": x}, { [x.to_string()]: x});
//println("Lambda", { x: 1 => x + 1 }(), { x: { x: }, y: { y: 2} => x.get("x") + y.get("y") }());
//println("Block", { 1 + 2 }, { {x:}.get("x") + { y: 2}.get("y") });

match ([1, 2]) {
    [0, if_zero] | [1, if_one] => { if_zero + if_one }
}
