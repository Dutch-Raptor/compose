let a = 4;
let a = [1, 2, 3] is [a, b, c] && a + b + c == 6;
{
    a;
    let a = a + 1 + b;
    a;

    match (a is int x) {
        true if true is bool y => x && y,
        false => x,
    }

    while (a is a x && x < 10) {
        a = x + 1;
    }

    let f = { |a, print| b: a + 1, [c, d] =>
        a + b
    }

    let mut b = 4;
    for (a in 0..b) {
        b = f(b: b, a, f, y);
    }

    b;

    if (a is Int x) {
        x;
    }
    else if (a is String x) {
        x;
    }
    else {
        a;
    }
}
print a